# CST 0.2.0 릴리스 노트 (최초 빌드)

**CST (CoC JP Scenario Translator)** — 일본어 Call of Cthulhu 시나리오 번역·문맥교정 보조 도구
배포일: 2026-06-01 · 배포 파일: `CST-0.2.0-portable.exe` (Windows 무설치판)

---

## 이런 프로그램입니다

일본어 CoC 시나리오를 한국어로 번역하고, 한국어 시나리오를 GM이 읽기 좋게 다듬어 주는 데스크톱 도구입니다. **번역기**와 **문맥교정기** 두 도구를 상단 버튼으로 오가며 쓸 수 있고, 두 도구는 같은 API 설정을 공유합니다. OpenAI 또는 Gemini 중 하나를 골라 본인 API 키로 사용합니다. (둘 중 하나만 있으면 됩니다.)

## 이런 기능이 있습니다

### 번역기
- 일본어 원문을 한국어로 번역하고, 결과를 **번역문 / CoC 7판 참고 메모 / 검토 필요 항목 / 고유명사·호칭 메모**로 나누어 보여 줍니다.
- 번역 모드 4종: **원문 충실 · 자연스러운 번역 · 핸드아웃 보존 · CoC 7판 메모 중심**. 문장 성격에 맞게 고를 수 있습니다.
- **보조 메모**에 인물명·호칭·고유명사 표기·관계를 적어 두면 번역에 반영됩니다.
- **1회 번역 글자수 제한**으로 한 번에 보내는 양을 조절하고, 번역 도중 **중지**할 수 있습니다.
- 결과는 항목별 **복사** 또는 **전체 복사**로 바로 가져갈 수 있습니다.

### 문맥교정기
- 이미 한국어로 된 시나리오(번역 결과 포함)를 GM이 읽기 좋게 정리합니다.
- **지칭 변환**(예: 탐사자→당신)을 API 없이 즉시 처리합니다.
- **문장·문맥 정리**(GM 지문 / 대사 유지, 서술 보강 강도 3단계)와 **화자 개입 제거**를 지원합니다.
- **대사/서술 분리** 미리보기에서 블록 카드별로 수정·복사할 수 있습니다.
- 채워진 버튼은 제공사 API 호출(시간·비용 발생), 외곽선 버튼은 로컬 즉시 처리로 구분해 표시합니다.

### CoC 자동 변환
- 신화 생물·신격 이름을 한국 정발(초여명 7판 수호자 룰북) 명칭으로 변환합니다. (목록에 없으면 음역 후 확인 필요 표시)
- TRPG 진행 용어를 변환합니다. (예: KP→키퍼, HO→핸드아웃, 秘匿HO→은닉 핸드아웃)
- CoC 서적 약칭을 정식 서적명으로 바꾸고, 정발본이 있으면 한국어 제목과 일본 원제를 함께 표기합니다.
- 자동 확정이 어려운 항목(피해 보너스, 광기 처리, 추격 규칙, 주문, 크리처 스탯 등)은 **검토 필요 항목**에 모아 줍니다.

### API · 모델
- **OpenAI / Gemini** 제공사를 선택하고, 제공사별 API 키를 각각 저장합니다. 키를 둘 다 넣어 두고 제공사만 바꿔 쓸 수 있습니다.
- **모델 새로고침**을 누르면 현재 사용 가능한 모델만 불러옵니다. (단종 모델 자동 제외, 신규 자동 추가 — 별도 업데이트 불필요)
- 인앱 사용설명서에 **API 키 발급 따라하기**(Gemini·OpenAI)를 단계별로 담아, 처음 쓰는 사람도 보고 따라 할 수 있습니다.

### 화면 · 편의
- 테마 5종(라일락 민트·블루·핑크·그린·다크), 본문 글자 크기 조절, 설정 패널 접기/펼치기를 지원하며 선택값이 저장됩니다.
- 설정을 저장하거나 API 키를 삭제하면 설정 제목 옆에 상태 알림이 잠깐 나타났다 사라집니다.

### 안전 · 로그
- **오류 로그**는 내 PC에만 기록됩니다. API 키·입력 원문·번역/교정 결과 전문은 로그에 남지 않아, 문의 시 로그를 그대로 전달해도 안전합니다. (폴더 열기·복사·삭제 지원)
- **업데이트 확인**은 GitHub의 버전 정보만 조회합니다. 파일을 자동으로 내려받거나 실행하지 않으며, 교체·설치는 사용자가 직접 합니다.

## 설치 / 사용

1. `CST-0.2.0-portable.exe`를 실행합니다. (설치 불필요)
2. 설정에서 API 제공사 선택 → API 키 입력 → 모델 새로고침 → 모델 선택 → **설정 저장**.
3. 짧은 문장으로 먼저 테스트한 뒤 사용합니다. 자세한 발급법은 프로그램 내 **사용설명서 › 2. API 키 발급 따라하기** 참고.

## 알아두기

- API 키는 본인이 직접 발급합니다. 제작자가 키를 제공하거나 요금을 대신 부담하지 않습니다.
- API 키는 비밀번호처럼 다루고, 본인 키가 들어간 상태로 프로그램을 재배포하지 마세요.
- 무설치판은 프로그램을 완전히 종료한 뒤 새 exe로 교체하면 같은 Windows 계정의 기존 설정·키가 유지됩니다.

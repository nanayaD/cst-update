{
  "name": "coc-jp-scenario-translator",
  "version": "0.2.2",
  "description": "Japanese Call of Cthulhu scenario partial translator with Korean output and CoC 7th edition notes.",
  "main": "src/main.js",
  "author": "nanayaD",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/nanayaD/cst-update.git"
  },
  "bugs": {
    "url": "https://github.com/nanayaD/cst-update/issues"
  },
  "homepage": "https://github.com/nanayaD/cst-update#readme",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js && node --check src/manualContent.js && node --check src/translatorService.js && node --check src/editorService.js && node --check src/pdfService.js && node --check src/errorLogger.js && node --check src/updateService.js && node --check src/providers/openaiProvider.js && node --check src/providers/geminiProvider.js && node --check src/data/mythosGlossary.js && node --check src/data/trpgTerms.js && node --check src/data/cocBooks.js",
    "build:portable": "electron-builder --win portable",
    "build:installer": "electron-builder --win nsis"
  },
  "devDependencies": {
    "electron": "^42.3.3",
    "electron-builder": "^26.15.2"
  },
  "build": {
    "appId": "local.coc-jp-scenario-translator",
    "productName": "CST",
    "artifactName": "${productName}-${version}-${os}-${arch}-${name}.${ext}",
    "files": [
      "src/**/*",
      "package.json",
      "assets/**/*",
      "node_modules/pdfjs-dist/**/*"
    ],
    "asarUnpack": [
      "node_modules/pdfjs-dist/**"
    ],
    "win": {
      "icon": "assets/CoC-Scenario-Translator.ico",
      "signAndEditExecutable": false,
      "target": [
        "portable",
        "nsis"
      ]
    },
    "portable": {
      "artifactName": "CST-${version}-portable.${ext}"
    },
    "nsis": {
      "artifactName": "CST-Setup-${version}.${ext}",
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "deleteAppDataOnUninstall": false
    }
  },
  "dependencies": {
    "pdfjs-dist": "^4.10.38"
  }
}

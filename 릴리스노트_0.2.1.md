# CST 0.2.1 릴리스 노트

**CST (CoC JP Scenario Translator)** — 일본어 Call of Cthulhu 시나리오 번역·문맥교정 보조 도구
배포일: 2026-06-02 · 배포 파일: `CST-0.2.1-portable.exe` (Windows 무설치판)

---

## 이번 업데이트 요약

PDF에서 본문 텍스트를 뽑아내는 **PDF 추출** 기능이 새로 들어갔습니다. 일본어 CoC 시나리오 PDF의 텍스트를 번역기·교정기에 붙여넣기 좋게 만드는 보조 기능입니다. 기능 추가 외에 0.2.0에서 정리한 설정·로그 폴더 이름(CST) 변경도 함께 포함합니다.

## 새 기능: PDF 텍스트 추출

상단 **PDF 추출** 탭에서, 텍스트로 작성된 **가로쓰기** 시나리오 PDF의 본문을 뽑아 **UTF-8 TXT 파일**로 저장할 수 있습니다. 모든 처리는 내 PC에서만 이루어지며, PDF 파일은 어디에도 업로드되지 않습니다.

- **사용 순서**: ① PDF 파일 선택 → ② 추출 옵션 확인 → ③ 텍스트 추출 → 미리보기에서 확인·수정 → TXT 저장 또는 전체 복사.
- **추출 옵션** (모두 기본 켜짐, 바꾸면 다시 추출):
  - **루비/후리가나 괄호 합치기** — 한자 위 읽기 가나를 괄호로 합칩니다. 예: `探索者(たんさくしゃ)`
  - **일본어 글자 사이 공백 제거** — 글자마다 끼어 나오는 불필요한 공백을 정리합니다. (영문·숫자 사이 공백은 보존)
  - **페이지 줄바꿈 정리** — PDF 모양대로 끊긴 줄을 문단 단위로 합칩니다. 짧게 끝난 줄·들여쓰기·큰 간격은 문단으로 나눕니다.
- 추출 중 **진행률 표시**와 **중지**를 지원합니다.
- 텍스트가 비어 있으면 이미지(스캔) PDF이거나 텍스트 레이어가 없는 경우로 안내하며, 암호가 걸린 PDF는 추출할 수 없습니다.

### 적합한 PDF / 한계

- **적합**: 텍스트로 작성된 가로쓰기 단일 단 시나리오 PDF.
- **부적합/주의**: 이미지(스캔)로 만들어진 PDF, 세로쓰기 PDF, 다단·표가 섞인 복잡한 레이아웃은 추출되지 않거나 순서가 어긋날 수 있습니다. 루비 인식과 줄바꿈 정리는 PDF에 따라 완벽하지 않을 수 있으니, 결과가 이상하면 해당 옵션을 끄고 다시 추출하세요.

## 유지보수 (0.2.0 → 0.2.1 공통)

- 설정·로그 폴더 이름을 **CST**로 정리했습니다. (기존: `coc-jp-scenario-translator`)
- 0.2.0 사용자는 업데이트 후 첫 실행 시 기존 설정·API 키가 자동으로 이전됩니다. 키를 다시 입력할 필요가 없습니다.
- 이전이 정상적으로 끝나면 옛 폴더(`%APPDATA%\coc-jp-scenario-translator`)는 자동으로 삭제됩니다. 안 쓰는 위치에 API 키 사본이 남지 않도록 하기 위함이며, 새 폴더로 복사가 확인된 뒤에만 삭제합니다.

## 설치 / 사용

1. `CST-0.2.1-portable.exe`를 실행합니다. (설치 불필요)
2. 번역·교정 기능은 기존과 동일합니다. PDF 추출은 상단 **PDF 추출** 탭에서 사용합니다.
3. 자세한 사용법은 프로그램 내 **사용설명서 › 10. PDF 텍스트 추출** 참고.

## 알아두기

- PDF 추출은 API를 사용하지 않는 로컬 기능입니다. 번역·교정에는 기존과 동일하게 본인 API 키가 필요합니다.
- 무설치판은 프로그램을 완전히 종료한 뒤 새 exe로 교체하면 같은 Windows 계정의 기존 설정·키가 유지됩니다.
